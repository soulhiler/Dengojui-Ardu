# Ориентация/локализация по Wi-Fi для робота (исследование 2026)

> Можно ли использовать встроенный Wi-Fi XIAO ESP32-S3 для ориентации робота:
> по сигналу до роутера (RSSI), по времени пролёта (FTM), по отражённому сигналу
> (CSI). Decision-oriented, с реальными цифрами и ESP32-спецификой.
>
> Сверено с ESP-IDF доками, IEEE/arXiv-замерами на ESP32, реальными проектами.
> 403-блокировки → часть из выжимок, несущее подтверждено ≥2 источниками.

## Главный вывод (рамка, которую нельзя обойти)

**Wi-Fi даёт ПОЗИЦИЮ, а не КУРС.** Направление (куда робот смотрит) из Wi-Fi
статически не вытащить: для угла прихода (AoA) нужна фазо-когерентная антенная
**решётка**, а у ESP32 **одна антенна** (эталон «видеть Wi-Fi» — ESPARGOS из
**8 синхронизированных** чипов). Поэтому:

- **Курс/ориентация — это работа IMU (MPU6050) + одометрии**, не Wi-Fi.
- Роль Wi-Fi у нас — **абсолютная позиция без дрейфа**: одометрия+IMU точны
  коротко, но накапливают ошибку; Wi-Fi даёт шумный, но **не уплывающий** фикс,
  который **сбрасывает накопленный дрейф** и отвечает «в какой я комнате» /
  восстанавливает позу после «похищения» (kidnapped robot).

«Направление» из Wi-Fi возможно лишь как **курс движения** (вектор смещения
между позициями во времени) — и то это не ориентация корпуса, а направление пути.

## Три режима — реальные цифры и ESP32-S3

| Режим | Что даёт | Точность (реально) | Железо | Вердикт под нас |
|-------|----------|--------------------|--------|-----------------|
| **RSSI** | сила сигнала AP → дистанция/отпечаток | трилатерация ~2 м (нужно ≥3 AP с известными коорд.); отпечаток «комната» ~75–100% | **0** (есть) | ✅ для грубого фикса/комнаты |
| **FTM** (802.11mc) | время пролёта → дистанция | ~1.5–3 м indoor после калибровки (~5 м сырой) | ESP32-S3 маяки | ⏳ апгрейд (расставить маяки) |
| **CSI** | фаза/амплитуда по поднесущим (мультипас) | суб-метр в лаборатории, но per-room ML + сервер | 0 (но AoA — нет) | ❌ кроличья нора для навигации |

### RSSI (по сигналу до роутера/AP)
- `WiFi.RSSI()` — связанный AP; `WiFi.scanNetworks()` → RSSI всех видимых (в
  квартире обычно **5–25 AP**). Скан **1.5–4 с** и **роняет связь** со станцией —
  делать асинхронно/«скан-потом-реконнект».
- Шум: ±10–20 дБ стоя на месте (одна антенна, мультипас) → «высокоточная оценка
  невозможна». Трилатерация ~2 м, **но дома обычно 1–2 своих AP** (< нужных 3 с
  известными координатами) → чистая трилатерация плохо подходит.
- **Отпечаток (fingerprinting)** надёжнее трилатерации в мультипасе: карта
  «координата → вектор RSSI», потом k-NN. Реальные ESP32-проекты: ~75–100% точность
  «в какой комнате» (ani8897 ~2 м; joaocarvalho ~75% по комнатам).

### FTM (по времени пролёта, 802.11mc)
- **ESP32-S3 поддерживает FTM** (классический ESP32 — НЕТ, это главная ловушка).
  API `esp_wifi_ftm`, примеры initiator/responder.
- **ESP↔ESP работает без FTM-роутера** (домашние роутеры FTM обычно не умеют):
  расставить **свои ESP32-S3 маяки** (SoftAP-responder) в известных точках, робот
  (initiator) меряет дистанцию к каждому → трилатерация. Espressif прямо советует
  ESP↔ESP (само-калибровка).
- Точность на ESP32 (peer-reviewed замер): открытое ~1 м; indoor 20 МГц ~40% <2 м,
  ~10% >8 м (мультипас); с on-device ML ~80% <1.5 м. Нужна калибровка нуля на маяк.
- Тоже **только дистанция, без курса**.

### CSI (по отражённому сигналу)
- ESP32 отдаёт CSI (`esp_wifi_set_csi`, тулкиты esp-csi / ESP32-CSI-Tool). Богаче
  RSSI — ловит мультипас/движение.
- **Реально на одном чипе:** присутствие/движение/дыхание («кто-то в комнате»).
  Локализация по CSI-отпечатку бьёт RSSI (суб-метр в статье), но требует **сервер +
  ML, тренировку под каждую комнату**, хрупко к перестановке мебели, фаза шумная.
- **AoA / «видеть отражения для ориентации» — невозможно на одной антенне**
  (ESPARGOS = 8 синхронных чипов; робо-CSI-SLAM как WiROS/ViWiD — на многоантенных
  картах). Идея «курс по отражённому сигналу на ESP32» — не нашлось ни одного
  рабочего проекта, спекулятивно.
- Вердикт: для навигации/ориентации — плохой ROI; sweet spot одного чипа —
  детектор движения, не навигация.

## 🎯 Предлагаемый метод под наш робот (без покупок)

**Само-строящийся Wi-Fi-отпечаток, привязанный к нашей мировой модели → сброс
дрейфа.** Изящно ложится на то, что у нас уже есть (`spatial/world_model.py` —
персистентная модель в координатах; робот ездит с одометрией):

1. **Сбор на ходу (бесплатно):** в каждой позе робот делает `scanNetworks()` и
   пишет **вектор RSSI всех видимых AP**, тегируя его **своей координатой из
   world_model**. Так радио-карта строится **в собственных координатах робота,
   без ручного обмера** — побочный продукт обычного исследования помещения.
2. **Использование:** позже робот читает RSSI и матчит k-NN против карты →
   грубая **абсолютная позиция** (в координатах его же модели).
3. **Сплав:** одометрия+IMU(MPU6050) ведут позу коротко и дают **курс**; Wi-Fi-фикс
   раз в N секунд **ограничивает дрейф** и восстанавливает позу после потери
   (kidnapped/рестарт). Wi-Fi → поправка позиции в `PoseEstimator`, не курса.

Это даёт «грубый GPS внутри дома» **на уже имеющемся железе**, ровно для того,
чего не хватает одометрии — нескользящего абсолютного якоря. Точность комната/
пара метров — но именно для сброса дрейфа этого достаточно.

**Где жить коду:** скан и RSSI-вектор — на XIAO (отдавать в `/telemetry` или
новый `/wifiscan`); карта и k-NN — на сервере в `PoseEstimator` (в духе
«XIAO=источник, сервер=счёт»). Скан роняет связь → делать редко/асинхронно, или
на стоянках.

## Лестница апгрейда

1. **Сейчас:** RSSI-отпечаток ↔ world_model (метод выше) — 0 ₽, грубый сброс дрейфа.
2. **Точнее (~2 м):** 3–4 **ESP32-S3 FTM-маяка** в известных точках → трилатерация.
   Стоит ~$5–10 и развёртывания, но даёт стабильный метрический якорь.
3. **Не тратить силы:** CSI-AoA/«ориентация по отражениям» на одной антенне — тупик;
   CSI брать только если захочется **детектор присутствия** (другая задача).

## Итог одной строкой

Wi-Fi — это **якорь позиции против дрейфа**, не компас. Курс оставляем IMU+энкодерам;
Wi-Fi (сперва бесплатный RSSI-отпечаток в координатах нашей модели, потом опц.
FTM-маяки) сбрасывает накопленную ошибку и отвечает «где я в доме».

## Источники

- ESP-IDF FTM (чипы, ESP↔ESP, калибровка): https://github.com/espressif/esp-idf/blob/master/examples/wifi/ftm/README.md
- FTM на ESP32 (замеры, peer-reviewed): https://arxiv.org/html/2401.16517v1
- Классический ESP32 без FTM: https://www.esp32.com/viewtopic.php?t=19642
- Какие AP отвечают на FTM (MIT/Horn): https://people.csail.mit.edu/bkph/ftmrtt_aps
- RSSI-локализация ESP32 (~2 м, проект): https://github.com/ani8897/RSSI-based-Localization-using-ESP32
- RSSI ML по комнатам (~75%): https://github.com/joaocarvalhoopen/Indoor_WiFi_Localization_in_ESP32_using_Machine_Leaning
- esp-csi (официальный, motion/presence): https://github.com/espressif/esp-csi
- ESP32-CSI-Tool: https://github.com/StevenMHernandez/ESP32-CSI-Tool
- ESPARGOS (8 антенн для AoA — почему одна не годится): https://arxiv.org/pdf/2502.09405
- RSSI/одна антенна не даёт AoA: https://arxiv.org/pdf/2505.15835
- WiROS / ViWiD (робо-CSI-SLAM на многоантенных картах): https://wcsng.ucsd.edu/aarun/assets/files/wiros.pdf · https://arxiv.org/pdf/2209.08091

---

# Каталог умных решений Wi-Fi-позиционирования (глубокий обзор по регионам)

> Вторая волна: CN (CSDN/Zhihu/университетские журналы), US/EU-академия, индустрия.
> Цель — собрать самые изящные методы и отметить, что реально под наше железо.
> Цифры — из публикаций (часто best-case lab); считать ориентирами.

## A. Самые красивые алгоритмы (landmark)

| Метод | Идея в одну строку | Точность | Железо |
|-------|--------------------|----------|--------|
| **SpotFi** (Stanford) | 2D super-resolution (AoA×ToF, smoothed-MUSIC) из CSI: достаёт больше путей, чем антенн, и выделяет прямой луч | ~40 см, NLOS-устойчив | 3-антенный AP + CSI (Intel 5300), **не ESP32** |
| **Chronos** (MIT) | дециметр с ОДНОГО AP: перебор по частотным каналам синтезирует широкую полосу → ToF | дециметры, 1 AP | спец. прошивка AP |
| **Channel Charting** (Studer) | self-supervised: схлопывает CSI в позиционное многообразие **без меток** — карта сама себя организует | относительная карта | CSI; красиво ложится на наш авто-сбор |
| **WiFi-SLAM** (Ferris/Fox, GP-LVM) | строит радиокарту И локализацию одновременно, полу-/без учителя | — | commodity |
| **RatSLAM + WiFi** (CN) | биоинспирированный SLAM, Wi-Fi-отпечаток как «место» в гиппокамп-модели | ~0.97 м на роботе | робот + Wi-Fi |
| **esp-ppb** | беспроводная фазо-когерентная решётка из дешёвых ESP32-C3, синхронизация по FTM → AoA | AoA на дешёвых нодах | несколько ESP32-C3 (pre-prod) |

## B. Лучшее «без ручного обмера» (прямо наш случай)

- **PDR-автометки** (MCSLoc, CN): телефон/робот идёт, инерциалка метит RSSI траекторией, HMM+Viterbi кладёт на план — **карта строится сама**. ⭐ это валидация нашего метода (world_model вместо PDR даёт координаты).
- **Краудсорс + анти-загрязнение** (патент CN106714109A): кластеризация отсеивает плохие отпечатки, срок жизни записи (напр. 7 дней) бьёт временную деградацию сигнала.
- **Baidu**: deep-learning по краудсорс-логам, «无需人工指纹采集» (без ручного сбора); **Tencent**: майнит исторические логи позиционирования, обновляет ежедневно.
- **Manifold alignment / Channel Charting**: мало меток + много неразмеченных → восстановить позиции (тот же дух, что наш авто-сбор).

## C. Сплав WiFi + инерциалка (ядро точности, цифры впечатляют)

| Фьюз-фильтр | Результат |
|-------------|-----------|
| **UKF** (WiFi+PDR) | WiFi 7.76 м + PDR 4.57 м → **фьюз 1.24 м** |
| Частичный фильтр + **карта-ограничения** | частицы за стенами отсекаются (наш occupancy grid!) |
| Geomag + WiFi + PDR (адапт. ЧФ) | **1.02 м**, без инфраструктуры |
| Комплементарный фильтр | легче KF, для слабых CPU |

Паттерн всюду один: **WiFi = абсолютный якорь без дрейфа, инерциалка = гладкая относительная**; фильтр их взаимно лечит. Точно наш `PoseEstimator` с MPU6050.

## D. Дешёвые трюки RSSI (бесплатные улучшения)

- **SE-WKNN**: сравнивать **ранг** AP (Спирмен), а не абсолютный RSSI — устойчиво к дрейфу уровня/устройства. Чистый софт. ⭐ берём в наш k-NN.
- **Робастный Калман на RSSI** (Wuhan Univ.): отсев выбросов-мультипас → заметно точнее.
- **GPR/Kriging радиокарта**: интерполяция плотной карты из редких точек + дисперсия (уверенность) на воксель.
- **Кластеризация БД** (K-Means/KD-tree): coarse-to-fine матчинг, O(N)→быстро.

## E. Что НЕ брать на одну антенну
- SpotFi/CiFi/DeepFi/CSI-XGBoost — нужен 3-антенный Intel 5300 / Atheros, не ESP32.
- AoA-решётки — ESPARGOS (8 чипов) или esp-ppb (несколько нод). Одна антенна ESP32 — нет.
- UWB cm-уровня — отдельные модули (DWM3000), не Wi-Fi.

## Уточнённый метод под нас (с учётом каталога)

К предложенному выше («RSSI-отпечаток в координатах world_model») добавляем изящное:
1. **Матчинг — SE-WKNN** (ранговая корреляция RSSI) — устойчив к дрейфу, дёшев.
2. **Фьюз — частичный фильтр с ограничениями по occupancy-grid**: частицы вне свободных клеток отсекаются; WiFi-фикс + одометрия+MPU6050 (паттерн UKF/PF из раздела C). Ожидаемо ~1–2 м.
3. **Робастный Калман / медиана на RSSI** перед матчингом.
4. **На вырост:** Channel Charting (self-supervised карта без меток) идеально совпадает с нашим авто-сбором; FTM-маяки (ESP32-S3) для метрического якоря; esp-ppb если захочется AoA дёшево.

Итог: Wi-Fi остаётся **якорем позиции против дрейфа**, но теперь с конкретным изящным стеком (SE-WKNN + PF с карта-ограничениями + авто-сбор отпечатка в координатах модели), валидированным китайской и западной литературой (фьюз WiFi+инерциалка ~1–1.3 м).

## Источники (вторая волна)
- SpotFi (разбор): https://www.jianshu.com/p/63b8f8e1018a
- WiFi-PDR UKF (1.24 м, 测绘学报): https://html.rhhz.net/CHXB/html/2015-12-1314.htm
- Geomag+WiFi+PDR (1.02 м): http://tb.chinasmp.com/CN/10.13474/j.cnki.11-2246.2020.0170
- MCSLoc (PDR-автометки, crad.ict): https://crad.ict.ac.cn/cn/article/doi/10.7544/issn1000-1239.20200669
- Краудсорс анти-загрязнение (патент): https://patents.google.com/patent/CN106714109A/zh
- RatSLAM+WiFi (0.97 м): http://www.jos.org.cn/html/2019/11/5577.htm
- SE-WKNN: https://blog.csdn.net/navsense_support/article/details/146705649
- Робастный Калман RSSI (武大学报): http://ch.whu.edu.cn/HTML/2016-3-361.htm
- esp-ppb (фазо-когерентная ESP32-решётка): https://github.com/jonathanmuller/esp-ppb
- esp-csi (официальный): https://github.com/espressif/esp-csi
- ESP32 RSSI fingerprint (~1.5 м): https://blog.csdn.net/Master_H_ice/article/details/108718323
