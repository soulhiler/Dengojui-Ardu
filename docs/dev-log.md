# Журнал разработки «Деньгожуй»

Краткий лог по фазам роадмапа. Обновляется на каждом рубеже фазы
(commit + push фиче-ветки). Записи —
новые сверху. Инцидент безопасности — `docs/security-history-cleanup.md`.

---

## 2026-06-11 — Большая сессия: плата ожила, джойстики починены, самообновление приложения

### Плата (запасная XIAO, fw 1.2.4)

- Подключена по USB → **COM6**, MAC `1C:DB:D4:76:24:24`. Прошита текущим
  исходником `main` (1.2.4) — до этого на ней стоял старый билд, который
  не печатал JSON-телеметрию в Serial без Wi-Fi.
- Wi-Fi: сначала плата не держала ассоциацию (reasons 3/14/36 — почерк
  инцидента 2026-06-05, роутер), после перезагрузки роутера получила
  `192.168.9.9`. Проверено на железе: `/drive` (вкл. повороты), `/beep`,
  `/telemetry`, ToF живой.
- Примечание: ПК теперь на `192.168.9.18` (IP сменился с .12) —
  `camera_ip.txt` локальный файл, не в репо.

### Дашборд (`tools/xiao_serial_telemetry.py`)

- **КРИТИЧЕСКИЙ ФИКС прокси `/board/*`**: кэш-бастер вырезался срезом
  `split("&r=")` — а `r` это параметр ПРАВОГО колеса `/drive`. Запрос
  обрезался до `l=...`, прошивка дублировала левую команду на оба колеса:
  вперёд/назад «работали» случайно, повороты — нет. Теперь параметры
  парсятся по именам (`parse_qsl`), кэш-бастер переименован `r` → `_ts`.
- Джойстик: диаметр +20 % (168 px), стрелки направлений, живой индикатор
  `L · R`, ход кнопки 48→62 px.
- **Регуляторы как на UNO-стенде**: «Мощность» 40–255 (деф. 180) —
  масштабирует команды джойстика; «Пение (скважность)» 10–100 % (деф. 10) —
  уходит `gain` в `/beep`/`/melody`. Хранятся в localStorage.
- **HTTP-сервер стал многопоточным** (ThreadingMixIn + daemon + timeout 30 с
  на соединение): один зависший хендлер больше не морозит всю панель.
- Новые эндпоинты `/app/version.json` и `/app/apk` — раздача APK телефону.

### Android-приложение (v2)

- **ФИКС «одно нажатие и зависает»**: `onRelease` джойстика вызывал
  `drive.stopSending()` — это cancel() периодического driveJob, который
  запускается один раз в `connectAll()`. После первого касания цикл
  отправки умирал навсегда. Фикс: на отпускании только обнуляем команды,
  живой цикл сам шлёт `stop=1` (и кормит watchdog). Та же правка в кнопке
  «Стоп».
- Убран дублированный импорт CoroutineScope в `MjpegStream.kt`
  (Kotlin 2.0 в CI считает ошибкой).
- **Самообновление по LAN**: кнопка «Обновить» → `AppUpdater` качает
  `/app/version.json` + `/app/apk` с ПК и запускает установщик
  (FileProvider, REQUEST_INSTALL_PACKAGES). Версия = `dist/app-version.txt`
  (единый источник для gradle и дашборда), сейчас versionCode 2.
- **Фиксированный ключ подписи** `android/XiaoRobot/debug.keystore`
  закоммичен: раньше CI подписывал каждый билд случайным ключом и Android
  не ставил обновления поверх. Первая установка v2 — с удалением старого
  приложения (подпись сменилась), дальше — поверх.

### Прошивка

- `/beep` и `/melody` принимают `gain=10..100` → `xiaoAudioSetGain()`.
  Дефолт 10 % уже был в `xiao_motor_audio.h` (как на UNO).
  Скомпилировано (36 % флеша), НА ПЛАТУ ПОКА НЕ ЗАЛИТО — ждёт подключения.

### CI

- Джоба android не работала никогда: (1) `./gradlew` нет в репо (проект
  собирался только на Windows) → запуск wrapper напрямую
  `java -cp gradle/wrapper/gradle-wrapper.jar`; (2) у аккаунта исчерпана
  квота хранилища артефактов → APK публикуется в **rolling-release
  `apk-latest`** (релизы квоту не тратят), артефакт остался запасным
  каналом с continue-on-error.
- Ран #31 зелёный: brain ✓ firmware ✓ android ✓.

### Цикл доставки приложения (теперь)

```
правка кода → push → CI собирает APK → release apk-latest
  → APK скачивается в dist/ на ПК (закоммичен и в репо)
  → дашборд раздаёт /app/apk → телефон: кнопка «Обновить»
```

Файлы: tools/xiao_serial_telemetry.py, android/XiaoRobot/* (updater,
keystore, фиксы), xiao_cam_stream/xiao_cam_stream.ino (gain),
.github/workflows/build.yml, dist/app-version.txt, dist/xiao-robot-debug.apk.

---

## 2026-06-10 — Сведение двух линий разработки (Cursor `main` ← `claude/dev`)

Проект какое-то время развивался ДВУМЯ параллельными линиями от общего
предка `b283eab`:

- **`main` (работа из Cursor)** — плата ЖИВА (на запасной XIAO), прошивка
  доросла до **1.2.4** (build 15): ToF VL53L0X + радар + скан-360°, голос
  через моторы, HTTP-OTA, веб-джойстик для Arduino UNO, инструменты
  восстановления, отдельный сетевой инцидент 2026-06-05 (Wi-Fi ПК/роутер,
  НЕ смерть платы).
- **`claude/dev`** — фазы 1-5, `brain/` (ИИ-мозг), CI, отчёт о петле земли
  + защиты питания, одометрия/рефлекс в `xiao_drive.h`.

**Решение:** `main` — каноническая линия. В неё перенесено ценное из
`claude/dev` (ветка `claude/port-dev-into-main`):
- `brain/` + `docs/brain-api.md`, CI (`.github/workflows/build.yml`,
  firmware-job дополнен установкой VL53L0X), `LICENSE`, `docs/onboarding.md`,
  `.editorconfig`, `docs/security-history-cleanup.md`, этот `docs/dev-log.md`;
- `xiao_drive.h` — слиты одометрия/антидребезг/рефлекс безопасности
  (`claude/dev`) с аудио-хуками motor-audio (`main`);
- доки безопасности питания (этот журнал + раздел README +
  `docs/hardware/build_wiring.py` как референс цепи защиты).

**НЕ перенесено** (и почему): OLED-драйвер по SPI — его пин D10 конфликтует
с ToF SCL (на одном XIAO вместе не живут, ToF приоритетнее); простой
`arduino_motor_test` из `claude/dev` — на `main` расширенный стенд лучше.

**Статус платы:** исходная XIAO мертва (петля земли, см. запись 2026-05-26 —
остаётся как урок по безопасности питания); разработка идёт на ЗАПАСНОЙ
плате. Курс на одном XIAO; гибрид с Raspberry Pi («Путь B», исследование на
ветке `claude/russian-greeting-aN4wi`) — отложен.

Проверка: `brain` 9/9 тестов OK; прошивка `main` компилируется
(`esp32:esp32:XIAO_ESP32S3:PSRAM=opi`, 35% флеша) и до, и после мёрджа
`xiao_drive.h`.

---

## 2026-05-26 — ИНЦИДЕНТ: смерть платы XIAO ESP32-S3 Sense

### Симптомы (что наблюдалось)

После распайки робота по схеме `docs/hardware/build_wiring.py` и первой
подачи питания плата XIAO ESP32-S3 Sense **перестала отвечать**:

- Красный диод (CHG — индикатор зарядки) горит — питание на входе есть.
- **Жёлтый диод (USB activity) не загорается** ни разу — ни при подаче
  питания, ни при нажатии R (RESET), ни при удержании B (BOOT).
- В Windows USB-устройство в статусе `CM_PROB_PHANTOM`
  (`pnputil`: «The device is not connected»). Drivers помнят прошлое COM5,
  но физического подключения нет.
- `arduino-cli upload --port COM5` → `Could not open COM5, the port is
  busy or doesn't exist`.
- Bootloader-режим (B+R) тоже не даёт COM-порт — то есть hardware-bootloader
  не отвечает.
- **WiFi-стек не поднимается** ни от USB-питания, ни от батареи: на сети
  не появляется ни одного устройства с MAC-префиксом Espressif (проверено
  ping-сканом /24 + ARP, до подключения и после, +18 сек ожидания на
  батарее).
- mDNS `xiao-cam.local` молчит.
- Все эндпоинты (`/telemetry`, `/stream`, `/drive`, OTA 3232) — недоступны.

### Контекст (что делали)

- Свежераспаянная сборка: батарея 7.4–8.4 В (2S Li-Ion) → DC-DC Buck → 5 В →
  XIAO 5V pin; TB6612FNG; OLED SSD1306 SPI; 2 мотора.
- В какой-то момент **USB-кабель к ПК и батарея 7.4 В были подключены
  одновременно** через общий минус (это подтверждено пользователем).
- Pin-конфликт в прошивке (D6/D7/D8 у энкодеров vs OLED) был исправлен,
  но новая прошивка **не успела залиться** — на плате осталась прежняя.

### Аудит моей схемы (build_wiring.py)

Топология схемы **корректна**:
- `BAT+` → `TB6612 VM` (питание моторов; TB6612 терпит до 15 В, наша 7.4–8.4 В безопасна).
- `BAT+` → `Buck IN+` (вход понижайки).
- `Buck OUT+` (5 В) → `XIAO 5V` pin (питание логики через VBUS-цепь XIAO).
- `XIAO 3V3` (выход внутреннего LDO) → `TB6612 VCC`, `TB6612 STBY`,
  `OLED VCC`, `OLED RES`.
- Общая земля: `BAT−` + `Buck IN−`/`OUT−` + `TB6612 GND` (3 пина) +
  `XIAO GND` + `OLED GND` — всё в одну шину.
- Моторы: `AO1/AO2`→`Motor L`, `BO1/BO2`→`Motor R`.
- 3.3-вольтовые сигналы: PWMA/AIN1/AIN2/PWMB/BIN1/BIN2 + SCK/MOSI/DC/CS.

**Однако в схеме отсутствовали ключевые защиты:**
1. **Нет предупреждения** о смертельной опасности одновременной подачи
   USB-питания (от ПК) и батарейного питания. Это самая частая причина
   смерти ESP32-плат у любителей, и в схеме это никак не отмечено.
2. **Нет защитного диода** (Schottky или ideal-diode MOSFET) между
   `Buck OUT+` и `XIAO 5V`, который защищал бы XIAO от обратного тока
   при USB-питании, или наоборот — Buck от USB-VBUS.
3. **Нет TVS-диодов** на USB D+/D− линиях для защиты USB-PHY ESP32-S3
   от ESD/перенапряжения через петлю земли.
4. **Нет указания** «измерить выход Buck мультиметром ДО подключения
   к XIAO 5V». Buck-преобразователи на AliExpress часто приходят с
   потенциометром, выкрученным на 12+ В — без проверки можно сжечь
   плату с первой подачи.
5. **Нет предохранителя** (PPTC 1–2 A) на батарейном проводе для защиты
   от КЗ при пайке.

Это конструктивные пробелы моей схемы — записываю как ответственность
автора (меня), не пользователя.

### Гипотезы причины смерти (по убыванию правдоподобия)

#### Гипотеза 1: Петля земли (USB ⇄ батарея одновременно) — вероятность ≈70%

**Механизм.** При одновременном подключении USB к ПК и батареи 7.4 В,
земли USB-хоста и батареи объединяются в точке `XIAO.GND` через провод
общего минуса. Если потенциалы GND отличаются (плохой блок питания ПК
с утечкой на корпус, или ёмкостная разность из-за зарядки/Li-Ion-схемы) —
течёт уравнивающий ток через USB GND. USB D+/D− линии у ESP32-S3
защищены слабыми ESD-диодами; уравнивающий ток >100 мА пробивает их.
Если разряд был сильным — пробивает не только USB-PHY, но и общую
3.3 В шину, через которую запитан весь SoC.

**Соответствие симптомам:**
- ✅ USB-CDC мёртв (USB-PHY повреждён первым).
- ✅ Жёлтый LED (USB activity) не загорается.
- ✅ Bootloader не отвечает (он использует тот же USB-PHY).
- ✅ WiFi не поднимается (SoC мёртв, не только USB-часть).
- ✅ Красный диод горит — он привязан к схеме зарядки на плате XIAO
  (отдельная микросхема, не SoC), и работает пока есть VBUS/батарея.
- ✅ Нет видимых повреждений — типично для электрической смерти через
  ESD-каналы (внутреннее, без дыма).

**Подтверждено в коде:** комментарий в `xiao_cam_stream.ino:13` —
«Красный у зарядки не программируется — смотри только жёлтый у USB».
То есть красный — это charge-IC LED, может гореть на мёртвом чипе.

#### Гипотеза 2: Нахлёст 5 В / 7.4 В — попадание 7.4 В на XIAO 5V pin — вероятность ≈15%

**Механизм.** При пайке провод `BAT+` (7.4–8.4 В) случайно ушёл на
`XIAO 5V` pin (вместо `Buck OUT+`). Внутренний 3.3 В LDO XIAO
(обычно XC6220A331MR) имеет абсолютный максимум на входе ≈6 В,
типовой рабочий — 5.5 В. При подаче 7.4 В:
- LDO моментально перегревается и сгорает.
- 3.3 В шина либо проседает (чип не работает), либо получает 5+ В
  через сгоревший LDO (чип сгорает).

**Соответствие симптомам:**
- ✅ Красный диод горит (charge-IC на плате толерантна до ~15 В).
- ✅ Жёлтый и WiFi не работают (SoC мёртв).
- ⚠️ Однако: на схеме `Buck OUT+` и `BAT+` идут к разным концам платы,
  спутать их физически сложно если паял по схеме внимательно.

**Как проверить теперь:** уже мёртвая плата не даст однозначного ответа.
На будущее — измерить мультиметром: между `XIAO 5V` pad и `GND` с
включённым Buck (без USB), должно быть 5.0 В ± 0.1 В.

#### Гипотеза 3: Buck выдал перенапряжение — вероятность ≈5–10%

**Механизм.** Типичный DC-DC Buck-модуль (MP1584, LM2596) приходит с
потенциометром в случайном положении. Без калибровки может выдавать
12 В или больше. На `XIAO 5V` (max 5.5 В) это смерть, сценарий идентичен
Гипотезе 2.

**Соответствие симптомам:** то же, что и Г.2.

**Профилактика:** **ВСЕГДА** измерять Buck выход мультиметром перед
подключением.

#### Гипотеза 4: Замыкание припоя — вероятность ≈5%

**Механизм.** Капля припоя замкнула: VBUS↔GPIO, или 3V3↔5V, или 5V↔GND.
При подаче питания узел перегрелся и сгорел.

**Как проверить:** визуальный осмотр пайки под лупой — особенно
плотные пины XIAO (шаг 2.54 мм, но дорожки тонкие).

#### Гипотеза 5: Обратная полярность батареи — вероятность ≈3%

**Механизм.** Перепутаны `BAT+` и `BAT−`. Обычно сопровождается
характерным щелчком, запахом, видимым подгоранием корпуса чипа.
Пользователь такого не описывал.

#### Гипотеза 6: ESD при пайке — вероятность ≈2%

**Механизм.** Статический разряд при пайке без браслета. ESD-защита
ESP32-S3 не безгранична.

#### Гипотеза 7: Перенапряжение на GPIO из выходов моторов — вероятность ≈1%

**Механизм.** Если AO1/AO2/BO1/BO2 (выходы H-моста) случайно
закоротились на 3.3-вольтовые GPIO XIAO. Маловероятно при правильной
разводке — выходы моторов идут НА моторы, не на XIAO.

### Окончательный вывод

Наиболее вероятна **Гипотеза 1 (петля земли)** — она объясняет полную
картину симптомов и согласуется с подтверждённым фактом одновременного
подключения USB+батареи.

**Гипотезы 2–3** (перенапряжение на 5 В pin) — вторая по правдоподобию,
но в нашем случае невозможно проверить постфактум на мёртвой плате.
Включаю в правила на будущее.

### Уроки и правила на будущее (PERMANENT)

1. **🛑 НИКОГДА не подключать USB и батарею одновременно.** Перед
   подключением USB к ПК — отсоединить батарею (или хотя бы её плюс).
   Перед подключением батареи — отсоединить USB. Если нужны оба
   источника одновременно (например, для прошивки во время полевых
   испытаний) — ставить **USB-isolator** (ADuM3160 или подобный) либо
   ideal-diode MOSFET-переключатель источников.

2. **📏 ВСЕГДА измерять выход Buck мультиметром ДО первого подключения
   к плате.** Цель: 5.0 В ± 0.1 В без нагрузки. Если выше — крутить
   потенциометр Buck. Если ниже 4.8 В или нестабильно — Buck дефектный
   или перегружен.

3. **🎨 Цветовая маркировка проводов питания, разная для разных
   напряжений:**
   - `BAT+` (7.4 В) — **толстый красный** или **жёлтый**.
   - `Buck OUT+` (5 В) — **тонкий красный** или **оранжевый**.
   - `3V3` — оранжевый (потоньше).
   - `GND` — **чёрный**, всегда и только.
   - Никогда не использовать один цвет для проводов разного напряжения.

4. **🔌 Защитный диод на 5 В входе платы.** Schottky (например, SS14)
   последовательно между `Buck OUT+` и `XIAO 5V`, анод к Buck:
   - Защищает XIAO от обратного тока с USB VBUS.
   - Защищает Buck от обратного тока с USB VBUS.
   - Падение ≈0.3 В — компенсировать настройкой Buck на 5.3 В.

5. **🔒 Предохранитель на батарее.** PPTC 1–2 А или плавкая на
   батарейном `+` проводе. Защищает от КЗ при пайке/коротком провода.

6. **🛡️ TVS-диоды на USB D+/D−** для защиты USB-PHY от ESD/
   перенапряжения через петлю земли. Готовые модули: USBLC6-2SC6,
   PRTR5V0U2X.

7. **👀 Перед первым включением — визуальный осмотр пайки под лупой.**
   Особенно у плотно расположенных пинов и силовых дорожек.

### Решение для проекта

Плата физически мертва, восстановлению не подлежит. Дальнейшие шаги:
- **A.** Заказать новую `Seeed XIAO ESP32-S3 Sense` (≈$10–15
  AliExpress, ≈1 неделя). При получении — собрать с учётом правил
  выше.
- **B.** Временно использовать `AI-Thinker ESP32-CAM` (есть у
  пользователя) с stock-прошивкой `CameraWebServer` для проверки
  «глаза» проекта. Полноценная адаптация нашей прошивки под ESP32-CAM
  не имеет смысла — другой чип, нет микрофона, недостаточно GPIO.

### Изменения в репозитории

- `docs/dev-log.md` — эта запись (полный отчёт о расследовании).
- `docs/hardware/build_wiring.py` — добавлен крупный комментарий-
  предупреждение в шапку файла с правилами 1–3.
- `README.md` — новый раздел «⚠️ Безопасность питания» сразу после
  «Состав».
- Memory: новое правило `feedback_power_safety.md` (USB+батарея
  одновременно = смерть) + регистрация в `MEMORY.md`.

---

## 2026-05-25 — OLED-драйвер в прошивке + фикс конфликта пинов D6/D7/D8

Пользователь собрал железо по схеме, но **OLED не работает и моторы не отвечают**.
Диагностика показала две причины:

**1. OLED-драйвера не было в прошивке вообще.** `grep -i 'oled\|ssd1306\|u8g2'` по
`xiao_cam_stream/` — 0 совпадений. Схема была нарисована корректно, но код
SSD1306 никто не писал. Добавил:
- `#include <U8g2lib.h>`
- Глобал `U8G2_SSD1306_128X64_NONAME_F_4W_SW_SPI gOled(R0, SCK=D8, MOSI=D10, CS=D6, DC=D7, reset=NONE)` —
  Software SPI, чтобы не разруливать ESP32-S3 HW-SPI remap.
- `oledInit()` после `xiaoDriveInit()` в `setup()`.
- `oledTick()` в `loop()` каждые 500 мс: IP/RSSI/channel, cmd_l/cmd_r/WD-флаг,
  счётчики кадров камеры, мкр-уровень в dBFS, temperatureRead°C.

**2. Конфликт пинов D6/D7/D8.** В `drive_config.h` пины энкодеров стояли как
`DRIVE_ENC_L_A=43, _B=44, R_A=7, R_B=8` — а это ровно те же D6/D7/D8, которые
схема отдаёт OLED под CS/DC/SCK. `xiao_drive.cpp` ставил `attachInterrupt` на
D7/D8, и любая SPI-транзакция U8g2 на тех же пинах сразу триггерила ISR
энкодеров → возможные виснущие прерывания, шум, фантомные движения, и
конкретно — OLED никогда не получал чистого clock. ISR-гарды в `xiao_drive.h`
(`#if DRIVE_ENC_L_A > 0`) корректные, поэтому достаточно занулить все четыре:
```
#define DRIVE_ENC_L_A 0
#define DRIVE_ENC_L_B 0
#define DRIVE_ENC_R_A 0
#define DRIVE_ENC_R_B 0
```
Комментарий-предупреждение в шапке зафиксирован: «энкодеры отключены — пины
отданы под OLED». Это решение явное (одна правка в одном месте), не неявное
«забыл подключить».

**Компиляция:** `arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi`
прошла, EXIT=0. Скетч 35% флеша (1.19 МБ из 3.34 МБ), глобалы 19% RAM.
Библиотека `U8g2@2.35.30` подтянулась автоматически в `tools/arduino-cli/.../libraries/`.

**Прошивка ОТЛОЖЕНА — USB-проблема:** плата на COM5 в статусе `CM_PROB_PHANTOM`
(`pnputil`: «The device is not connected»). Физическое USB-соединение не
живо — либо кабель power-only, либо плохой контакт Type-C, либо плата
повисла без USB CDC после прошлой прошивки. Решение — действия пользователя:
переподключить кабель / попробовать другой / зажать BOOT при подключении.

Как только USB вернётся — `arduino-cli upload --port COM5` и проверка
на железе (OLED должен показать IP сразу после подключения к WiFi; моторы
должны реагировать на `/drive?l=120&r=120` из Android-приложения).

Файлы: `xiao_cam_stream/xiao_cam_stream.ino`, `xiao_cam_stream/drive_config.h`.

---

## 2026-05-25 — Схема пайки: переход на schemdraw (Python), стиль KiCad

После четырёх итераций с Graphviz (rankdir/splines=ortho/xlabel/rails) —
auto-routing всё равно клал провода через корпуса и метки на пины. Это
**фундаментальное ограничение Graphviz** для wiring-диаграмм, не моя ошибка.

**Решение: `schemdraw`** (`pip install schemdraw matplotlib`) — Python-
библиотека специально для электронных схем (инженерные публикации,
KiCad-style). Ручная XY-разметка, нет auto-routing — полный контроль:
- Каждый компонент `elm.Ic(pins=[elm.IcPin(...)])` с явным side/slot
- Провода `elm.Wire('-|').at(a).to(b)` — L-образные, я выбираю стороны
- Net-теги `elm.Tag().label("3V3", color=...)` — пилюли у пина

**Архитектура схемы (KiCad-style):**
- Прямые провода — **ТОЛЬКО для коротких 2-pin сигналов** (BAT+→VM,
  BUCK 5В→XIAO 5V, AO/BO→моторы)
- Для всех остальных сигналов — **NET-LABELS у каждого пина с обеих
  сторон** (PWMA/AIN1/AIN2/BIN1/BIN2/PWMB/SCK/MOSI/DC/CS/3V3/GND).
  Имена совпадают = сигналы соединены, рисованных проводов нет.
- Цвет провода/тега = функция; легенда внизу.

**Результат:** настоящий принципиальный чертёж — никаких пересечений,
никаких наложений на пины, всё в физическом порядке силкскрина.

Исходник: `docs/hardware/build_wiring.py` (Python ~200 строк, читаемо
и редактируемо). Заменил Graphviz `wiring.gv` на этот скрипт.
Output: PDF/PNG/SVG с одной командой `python build_wiring.py`.

**Memory:** правило `feedback_wiring_diagrams.md` финализировано —
для любых wiring/pinout-диаграмм используем schemdraw, не Graphviz.

---

## 2026-05-25 — Схема пайки: убраны xlabels, цвет = функция (финал)

Подписи `xlabel="D0→AIN1"` и т.п. на сигнальных проводах с
`splines=ortho` Graphviz кладёт поверх пинов соседних компонентов
(в данном случае на пинах XIAO). Это и было то самое «провода
накладываются на пины», что заметил пользователь.

**Решение по конвенции принципиальных схем:** функцию провода несёт
**цвет**, не текст. Пины на обоих концах уже подписаны (D5, PWMA);
текстовая подпись на самом проводе избыточна и в нашем случае только
мешает. Убрал `xlabel` со всех сигнальных рёбер, оставил только `color`.
Добавил **развёрнутую цветовую легенду** в углу схемы:

- Красный = VM / 5 В питание
- Зелёный = PWM
- Синий = IN1/IN2 направление
- Серый = выходы моторов AO/BO
- Фиолетовый = SPI данные (SCK/MOSI)
- Розовый = SPI управление (CS/DC)

**Memory rule расширен:** «На принципиальных схемах функцию сигнала
несёт ЦВЕТ; xlabels на проводах с ortho-routing накладываются на пины —
не использовать. Цветовая легенда обязательна».

Финальный вид — чистый инженерный чертёж: net-теги питания, цветные
безтекстовые сигнальные провода, легенда. Применяется автоматически.

---

## 2026-05-25 — Схема пайки: net-labels (как в KiCad/Altium), без шин

Версия с rail-узлами (3V3/GND как горизонтальные полосы) дала пересечения
и наложения проводов в центре схемы — из-за конвергенции всех отводов
в один узел-шину. Это не соответствует инженерным стандартам.

**Финал — net-labels (стандарт KiCad/Altium/EAGLE):**
- Никаких bus-узлов и длинных шин не рисуем.
- У КАЖДОГО пина-потребителя 3V3 или GND стоит **отдельный маленький
  тег-пилюля** с именем сети (`3V3` оранжевый, `GND` чёрный) сразу
  рядом с пином, через короткий отвод.
- Пины с одинаковой меткой считаются электрически соединёнными
  (читатель домыслит — это стандартная конвенция, явно описана в
  легенде вверху схемы).
- `rank=same` для каждой группы (XIAO+теги, TB6612+теги, OLED+теги
  и т.д.) — теги стоят рядом со своим компонентом, не уплывают.
- Сигнальные провода 2-pin (D5→PWMA, AO→Motor) — остаются прямыми
  подписанными рёбрами.

**Memory:** правило `feedback_wiring_diagrams.md` дополнено: «при 3+
потребителях общей сети — НЕ рисовать bus-узел с отводами; использовать
net-labels у каждого пина (отдельный тег с именем сети, без edges
между ними)».

Результат — компактная схема без пересечений и наложений; визуально
читается как настоящий принципиальный чертёж.

---

## 2026-05-25 — Схема пайки: переход на инженерные стандарты (rails + ortho)

После двух итераций провода всё ещё пересекали корпуса. Применил полные
конвенции принципиальных схем (KiCad / Fritzing / IEEE 315):

1. **Power/GND rails** вместо fan-out: добавлены два узла-«шины» —
   `3V3 — питание логики` (оранжевая полоса) и `GND — общая земля`
   (чёрная полоса). XIAO 3V3 «питает» 3V3-шину, остальные потребители
   (TB6612 VCC/STBY, OLED VCC/RES) короткими отводами от шины. Все 6
   GND-пинов сводятся в GND-шину одиночными отводами. Это убрало 8
   длинных fan-out проводов, которые до этого пересекали полстраницы.
2. **Orthogonal routing** (`splines=ortho`) — все провода под 90°.
3. **`xlabel=` вместо `label=`** для рёбер: ortho в Graphviz не
   поддерживает обычные label на edges (warning при рендере); xlabel
   («external label») с этим режимом работает.
4. **Compass на ВСЕХ концах рёбер** указывает на внешнюю грань ячейки
   (правило сохранено в памяти: `feedback_wiring_diagrams.md`).
5. **Сигнальные провода 2-pin** (D5→PWMA, AO1→Motor L, ...) — остаются
   прямыми подписанными рёбрами; в шины не загоняем.

Результат: провода больше **не проходят сквозь корпуса**, шины
питания/земли видны как горизонтальные рельсы, метки на местах.
Источник `wiring.gv` ~190 строк, PDF ~67 КБ, PNG ~167 КБ.

**Memory:** правило wire-diagram-rules расширено секцией о rails,
ortho, 2-pin direct wires. Применяется автоматически в будущих
задачах wiring/pinout.

---

## 2026-05-25 — Схема пайки: физическая раскладка пинов + DC-DC Buck 5В

По фидбэку:

1. **Пины компонентов теперь в том же порядке, что на плате (силкскрин):**
   - **TB6612** — горизонтальный 8 × 2 ряда (раскладка SparkFun-корпуса):
     верх — `VM | VCC | GND | AO1 | AO2 | BO2 | BO1 | GND` (мотор-сторона),
     низ — `PWMA | AIN2 | AIN1 | STBY | BIN1 | BIN2 | PWMB | GND` (логика).
   - **OLED** — 1 ряд × 7 пинов: `GND | VCC | D0/SCK | D1/MOSI | RES | DC | CS`.
   - XIAO уже совпадал с физикой (2 колонки × 7, USB-C сверху).

2. **Добавлен DC-DC Buck 5В** между батареей и XIAO:
   - Батарея 7.4 В → TB6612 **VM** (питание моторов, прямо).
   - Батарея 7.4 В → Buck **IN+/IN−** → XIAO **5V/GND**
     (логика питается стабилизированными 5 В, не 7-8 В).
   - На схеме помечено `MP1584 / LM2596` — оба распространены и работают.

**Раскладка** — `rankdir=TB` (сверху вниз): верх — батарея/Buck/моторы,
центр — TB6612 горизонтально, ниже — XIAO, в самом низу — OLED.
Моторные провода идут с моторов сверху прямо вниз в AO/BO TB6612 — без
обходов корпуса.

Файлы (`docs/hardware/`): `wiring.gv` (источник), `wiring.png` (превью
для GitHub), `wiring-xiao-tb6612.pdf` (печать; ~372×354 мм, «fit to A4»).

---

## 2026-05-25 — Схема пайки: переход на Graphviz `dot` (HTML-таблицы + порты)

Ручная векторная отрисовка через reportlab.canvas давала наложения проводов
и кривое размещение. Поискал инструменты в сети, попробовал два:

- **WireViz** (`pip install wireviz`) — специализирован под harness-кабели,
  отлично рендерит «бандлы», но в нашем случае каждый кабель выводился
  отдельным боксом с таблицей (от→до), а это **дублирование** информации
  и визуальный шум.
- **Чистый Graphviz `dot`** — выиграл: каждый компонент — HTML-таблица
  с пинами-портами, каждый провод — обычное ребро `node:port → node:port`
  с цветом и подписью. Auto-routing без cable-боксов.

В итоге:
- Исходник `docs/hardware/wiring.gv` (~140 строк, читаемо).
- Рендер `dot -Tpdf wiring.gv -o wiring-xiao-tb6612.pdf` (~62 КБ, 1 страница,
  ~314×338 мм — печатается «fit to A4» нормально, остаётся читаемым).
- PNG-превью `wiring.png` рядом — отрисовывается в GitHub blob.
- Тулчейн: portable Graphviz 15.0.0 в `tools/graphviz/` (gitignored,
  скачка по аналогии с `arduino-cli`).

**Ключевая правка раскладки** (по фидбэку — «моторные провода путались»):
в таблице TB6612 левая колонка — VM/VCC/GND/STBY/PWM/IN (мордой к XIAO и
батарее), правая — AO1/AO2/BO1/BO2 (мордой к моторам). Компас-стороны
`:e/:w` на проводах форсируют выход с нужного бока — dot больше не водит
провода в обход корпуса.

---

## 2026-05-25 — Схема пайки: визуальный вектор вместо ASCII

PDF полностью переделан. Было: таблицы + ASCII-блок-схема. Стало:

- **Стр. 1 (A4 landscape):** настоящая векторная схема — все 6 компонентов
  как боксы с подписанными пинами (TB6612 слева, XIAO в центре, OLED справа,
  батарея + 2 мотора по краям). Каждый провод — отдельная цветная линия
  с подписью на ней (`D5 → PWMA`, `D8 → SCK` и т.д.). Цветовая легенда:
  красный = VM, оранжевый = 3.3 В, чёрный = GND, зелёный = PWM,
  синий = направление, фиолетовый = SPI, сиреневый = CS/DC, тёмно-серый = моторы.
  3V3 и GND нарисованы как горизонтальные «шины» с отводами к компонентам —
  одно решение, никаких звёзд по проводам.
- **Стр. 2 (A4 landscape):** табличный «список проводов» — каждая строка =
  отдельный провод (цвет-чип, откуда, куда, сигнал, GPIO, примечание).
  Всего 22 провода. Плюс 8-пунктный чек-лист перед подачей питания.

Источник пинов — `xiao_cam_stream/drive_config.h` (моторы) и принятое
размещение OLED на D6–D10 (3V3 для VCC и RES). Файл — тот же
`docs/hardware/wiring-xiao-tb6612.pdf` (98 КБ, 2 стр.); ссылка в README
не меняется.

---

## 2026-05-25 — Схема пайки расширена: + OLED SSD1306 (SPI)

К существующей схеме плата→TB6612→моторы добавлен раздел подключения
**OLED 0.96″ SSD1306 7-pin SPI** (вариант с резисторами 4SPI: R3, R4 — на физическом
модуле уже распаян именно так).

**Распиновка (XIAO → OLED):**
- VCC, RES → 3V3 (RES «всегда HIGH» — драйвер SSD1306 сам инициализируется)
- GND → GND (общая земля)
- CS  → D6 (GPIO 43), DC → D7 (GPIO 44)
- SCK → D8 (GPIO 7), MOSI → D10 (GPIO 9)

**Обоснование:** на гребне XIAO свободны только D6–D10 (D0–D5 заняты драйвером моторов).
Аппаратный SPI ESP32-S3 = D8/D10 → берём их. CS и DC — на D6, D7.
**RES сажаем на 3V3**, экономим 1 GPIO (для SSD1306 это штатно).

**Цена:** пины энкодеров (D6–D9) теперь у OLED — но энкодеров на телеге физически нет.
В прошивке надо отключить ISR-ы:
`DRIVE_ENC_L_A 0, DRIVE_ENC_L_B 0, DRIVE_ENC_R_A 0, DRIVE_ENC_R_B 0` в `drive_config.h`.

**Прошивка OLED пока не использует** — нужно подключить `U8g2` (или Adafruit_SSD1306):
`U8G2_SSD1306_128X64_NONAME_F_4W_HW_SPI(rotation, CS=D6, DC=D7, RST=U8X8_PIN_NONE)`.
Это отдельный шаг, по запросу.

Файл `docs/hardware/wiring-xiao-tb6612.pdf` перезаписан (2 страницы, ~125 КБ);
ссылка в README обновлена (теперь «плата → драйвер → моторы → экран»).

---

## 2026-05-20 — PDF-схема пайки (плата → TB6612 → моторы)

Добавлен печатный PDF с распиновкой для пайки. Источник пинов —
`xiao_cam_stream/drive_config.h` (без догадок): левый канал
D5/D0/D1 → PWMA/AIN1/AIN2 (GPIO 6/1/2), правый — D4/D2/D3 →
PWMB/BIN1/BIN2 (GPIO 5/3/4). Силовая часть TB6612: VM на батарею
4.5–13.5 В, VCC на 3V3 XIAO, общая земля обязательно, STBY = 3V3
(DRIVE_STBY_PIN = −1). Энкодеры и HC-SR04 — опционально, с
предостережением про конфликт D8/D9 с SD-картой и обязательным
делителем 5→3.3 В на ECHO. Резервные пины (камера/мик/LED) явно
перечислены.

- Файл: **`docs/hardware/wiring-xiao-tb6612.pdf`** (A4, 2 стр., ~120 КБ).
- Кириллица: Arial/Consolas из Windows подключены через
  `pdfmetrics.registerFont(TTFont(...))` в reportlab 4.5.
- Содержит таблицы XIAO↔TB6612, ASCII-блок-схему подключений, чек-лист
  перед подачей питания. Ссылка добавлена в `README.md`.

---

## 2026-05-19 — Полноэкранный UI + оверлеи + панель телеметрии

Жалоба: поля в приложении перекрывались.

- **Причина:** layout был вертикальной цепочкой ConstraintLayout
  (поля → видео 0dp → статус → джойстик); на устройстве цепочка
  схлопывалась и элементы налезали друг на друга.
- **Перестроено в полноэкранный режим:** видео `ImageView` =
  `match_parent` (фон), элементы — оверлеи поверх:
  верхняя панель (IP/токен/Подключить, скрывается после подключения),
  телеметрия слева, статус справа, джойстик снизу-слева,
  кнопки (звук/стоп) снизу-справа. Immersive: системные бары скрыты
  (свайп — показать), `onWindowFocusChanged` держит режим.
- **Поля больше не перекрываются:** оверлеи привязаны к углам/панели,
  а не в схлопывающейся цепочке; после подключения поля ввода прячутся
  (`showInputs`), видео на весь экран.
- **Телеметрия в приложении:** `TelemetryPoller` теперь отдаёт компактный
  многострочный блок (fw/темп, WiFi/RSSI/ch, IP, heap/мик dBFS,
  cam fail/кадры, привод L/R/enc/v, УЗ/safety/бампер) — оверлей
  слева, обновление каждые 2 с, устойчиво к отсутствующим полям.
- Файлы: `activity_main.xml` (переписан), `TelemetryPoller.kt`
  (переписан, один колбэк), `MainActivity.kt` (immersive, оверлей-
  телеметрия, скрытие полей). APK BUILD SUCCESSFUL, `dist/` обновлён.

---

## 2026-05-18 — APK положен в репо (скачивание на телефон)

Чтобы ставить APK без USB/проводов: собранный отладочный APK добавлен в
репозиторий — **`dist/xiao-robot-debug.apk`** (~7.2 МБ, сборка с mDNS).
Прямая ссылка для телефона добавлена в `README.md` (raw на ветке
`claude/dev`). Файл перезаписывается при пересборке (стабильное имя →
стабильная ссылка). Сборочные `android/**/build/` остаются в `.gitignore`;
в `dist/` кладём осознанно. Секретов в APK нет (`secrets.h` — только прошивка).

---

## 2026-05-18 — mDNS-автопоиск платы в приложении (IP больше не нужен)

По запросу: приложение само находит плату, IP вводить не надо.

- **Прошивка без изменений** — уже рекламирует `MDNS.begin("xiao-cam")` +
  `addService("http","tcp",80)` → сервис `_http._tcp` инстанс `xiao-cam`.
- **`BoardDiscovery.kt`** (новый): Android NSD — `discoverServices
  ("_http._tcp.")`, фильтр имени по «xiao», `resolveService` → текущий
  IPv4 платы. MulticastLock на время поиска; single-resolve guard
  (ограничение NsdManager до API 34); таймаут; всё в main-поток, один раз.
- **`MainActivity`**: поле пустое / `xiao-cam.local` / `auto` → mDNS-поиск
  (6 с); найдено → старт каналов на найденном IP; не найдено → пробуем
  `last_ip`, иначе просим ввести IP вручную. Литеральный IP → прямой путь
  (ручной фолбэк). `disconnect`/`onDestroy` останавливают поиск.
  Дефолт поля → `xiao-cam.local`; устаревший `.17` мигрирует на авто.
- **Manifest**: +`ACCESS_WIFI_STATE`, +`CHANGE_WIFI_MULTICAST_STATE`.
- Файлы: `BoardDiscovery.kt` (new), `MainActivity.kt`,
  `AndroidManifest.xml`, `strings.xml`, `activity_main.xml`.

**Сборка:** PowerShell-инструмент в сессии завис; APK собран
`java -cp gradle/wrapper/gradle-wrapper.jar
org.gradle.wrapper.GradleWrapperMain assembleDebug` → BUILD SUCCESSFUL,
Kotlin/NSD скомпилировались чисто (7.2 МБ). Поведение NSD на самом
телефоне — подтвердить на устройстве.

**Для пользователя:** поставить новый APK → просто «Подключить»
(IP не вводить; поле можно оставить `xiao-cam.local`).

---

## 2026-05-18 — Приложение слало на .17; дефолтный IP исправлен

**Симптом:** на экране статус `видео: Failed to connect to /192.168.9.17:80`;
поле IP не редактировалось (работали только кнопки). Браузер телефона по
`http://192.168.9.18/capture` — фото ОК (сеть и камера исправны).

**Причина:** дефолтный IP в приложении был захардкожен `192.168.9.17`
(и мог «прилипнуть» в SharedPreferences после прошлого Connect), а плата
по DHCP получила `192.168.9.18`. Приложение стучалось не туда. Трудность
с правкой поля усугубляла.

**Фикс (приложение):** дефолт хоста → `192.168.9.18`; авто-миграция
сохранённого устаревшего `.17` → `.18` (`MainActivity.onCreate`);
`activity_main.xml` дефолт-текст → `.18`. APK пересобран.

**Сборочное:** инструмент PowerShell в сессии завис (любые команды exit 1
без вывода), `gradlew.bat` через cmd не отрабатывал. APK собран
shell-агностично: `java -cp gradle/wrapper/gradle-wrapper.jar
org.gradle.wrapper.GradleWrapperMain assembleDebug` → BUILD SUCCESSFUL.

**Durable-рекомендация:** DHCP-резервация на роутере
(MAC `E8:F6:0A:8B:C3:84` → `192.168.9.18`) — тогда IP постоянный и поле
вообще не трогать. Опционально на будущее: Android NSD/mDNS
(`xiao-cam.local`), чтобы IP стал неважен.

**Установка:** удалить старое приложение → поставить новый APK → открыть →
«Подключить» (вводить ничего не нужно).

---

## 2026-05-18 — «Камера не подключается»: OV3660 QXGA→FB-OVF, исправлено

**Симптом:** плата в WiFi, но в приложении нет видео; `/capture` → HTTP 500.

**Диагноз на железе:** сенсор OV3660 (`cam_pid=0x3660`) был сконфигурирован на
**QXGA + jpeg_quality=2** (почти максимум). На непрерывном MJPEG это
переполняет буфер кадров → бесконечный `cam_hal: FB-OVF` →
`esp_camera_fb_get()` стабильно `NULL`. Телеметрия: `cam_fail`растёт,
`cam_frames_stream=0`, `/capture` отдаёт 500 «capture failed» (14 Б).
Т.е. ранняя пометка «FB-OVF безвреден» оказалась неверной под нагрузкой —
это и есть причина отсутствия видео (Фаза 2, всплыло только на железе).

**Фикс** (`xiao_cam_stream/initCamera`): стабильный потоковый профиль —
`config`: `FRAMESIZE_SVGA` + `jpeg_quality=12` (fb_count=2, GRAB_LATEST);
пост-init для OV3660/OV2640: `set_framesize(SVGA)` + `set_quality(12)`
(было QXGA/UXGA @ q2). Разрешение можно поднять позже, если поток стабилен.

**Проверка на железе (после reflash):** `FB-OVF=0`, `cam_fail=0`,
`/capture` → 200, **JPEG ~21 КБ** (3/3); `/stream` → ~1 МБ/4 с,
`cam_frames_stream=48` → **≈12 fps**. Плата `192.168.9.18`,
`Duangdeehouse2  2.4GHz`, RSSI −46. Камера и поток стабильны.

**Для приложения:** IP = `192.168.9.18`, телефон в той же 2.4 ГГц сети.

---

## 2026-05-18 — Видео/аудио в приложении не стартовали → плата выведена в WiFi

**Симптом:** на смартфоне приложение открылось, но видео и звук не идут.

**Диагноз (по serial, не догадки):** приложение ни при чём — **плата не в
WiFi**. Сначала была прошита с плейсхолдер-`secrets.h` (`YOUR_WIFI_PASSWORD`),
затем — с неверным написанием SSID. `wifi_status=disconnected`, `ip=0.0.0.0`.

**Решение:** временным скетчем-сканером считал эфир радио платы →
**точный SSID = `Duangdeehouse2  2.4GHz`** (ДВА пробела перед «2.4GHz»;
RSSI −41, WPA2). Прописал точный SSID + пароль в `xiao_cam_stream/secrets.h`
(gitignored, в репо не идёт; pre-commit хук тоже блокирует), перепрошил.

**Результат:** `wifi_status=connected`, **IP `192.168.9.18`**, RSSI −43.
Сквозная проверка по WiFi с ПК: `GET /telemetry` → 200,
`GET /capture` → 200, **JPEG 370 КБ**. `cam_fail=0` — простойный
`cam_hal: FB-OVF` подтверждённо безвреден (полный кадр отдаётся).

**Для приложения:** ввести IP **`192.168.9.18`** (дефолт в приложении
`192.168.9.17` — мимо на единицу), телефон в сети `Duangdeehouse2  2.4GHz`.
Альтернатива на будущее: mDNS `xiao-cam.local` или DHCP-резервация
(чтобы IP не «плавал»). Креды в git не попали (только этот dev-log).

---

## 2026-05-17 — Прошивка на железе + APK собран

Плата подключена по USB — прошили и собрали APK здесь.

- **Прошивка залита на плату** (COM5, FQBN `esp32:esp32:XIAO_ESP32S3:PSRAM=opi`):
  bootloader/partitions/app записаны, **hash verified**, hard-reset.
- **Boot подтверждён по USB serial**: телеметрия идёт, видны новые поля
  Фаз 2/3 — `cam_fail`, `spd_l`/`spd_r`, `drive_safety` (=0: сенсоры
  инертны/не разведены), микрофон активен (`mic_dbfs≈-27`), `chip_temp≈59°C`.
  Правки Фаз 1–3 работают на реальном железе (компиляция + базовый рантайм).
- **Known-issue:** на idle (без stream-клиента) спам `cam_hal: FB-OVF`
  (переполнение буфера кадров). **Не регрессия** — наш диф не трогал
  `grab_mode`/`fb_count`; типовое поведение esp32-camera без потребителя
  кадров. Проверить, что уходит при активном MJPEG/WiFi; кандидат на
  DMA-стриминг (вывод из веб-анализа esp32-cam-fpv).
- **Android APK собран:** поставлены JDK 17 (Temurin 17.0.19) + Android SDK
  (cmdline-tools, `platform-35`, `build-tools;35.0.0`, `platform-tools`;
  лицензии через hash-файлы; `local.properties` — gitignored).
  `gradlew assembleDebug` → **BUILD SUCCESSFUL**, `app-debug.apk` 6.0 МБ.
  Подтверждает Android-правки Фаз 1–3 (поле токена, reconnect/ошибки,
  индикаторы сенсоров).
- Снимает оговорку «прошивку/APK тут собрать нельзя» из ранних записей.
  Тулчейн (`arduino-cli`/JDK/SDK) и `secrets.h` — вне git (gitignored).

### Дальше (требует пользователя/железа)
Runtime-проверки в WiFi: MJPEG/латентность (и `FB-OVF` под нагрузкой),
cross-core I2S при `mic=0`, рефлекс «стоп», одометрия с моторами;
разводка УЗ/бамперов; user-gated чистка git-истории (пароль).

---

## 2026-05-17 — Верификация прошивки локально (arduino-cli)

На машине нашлись Arduino IDE-ядро и Python — прошивку удалось собрать здесь.

- `arduino-cli` 1.5.0 (portable в `tools/`, gitignored); переиспользовано уже
  установленное ядро `esp32:esp32` **3.3.8** из `Arduino15` через
  `ARDUINO_DIRECTORIES_DATA` — без повторной закачки ядра.
- `arduino-cli compile --fqbn esp32:esp32:XIAO_ESP32S3:PSRAM=opi xiao_cam_stream`
  → **успех, 0 ошибок, 0 предупреждений** в наших источниках.
- Размер: 1 181 569 Б флэш (**35 %**), глобальные 62 476 Б (**19 %**).
- `secrets.h` создан из example (gitignored, не коммитится).

### Что это даёт
Прошивочные правки **Фаз 1–3 подтверждены на уровне компиляции**: токен/
`apiAuthOk`, `initCamera` retry + `cam_fail`, `telemetryMicApplyState`
(I2S-release), антидребезг энкодеров + рефлекс безопасности + `spd_*`/
`drive_safety` в `/telemetry`. Это снимает прежнюю оговорку «в этой среде
собрать нельзя» для прошивки. Runtime/HW (cross-core I2S, рефлекс,
одометрия, реальные FPS) — по-прежнему требует платы.

---

## 2026-05-17 — Фаза 5 (гигиена и релиз) — роадмап завершён

- `LICENSE` — **MIT** (выбор владельца; правообладатель `soulhiler` —
  при необходимости поправить имя).
- CI `.github/workflows/build.yml`: 3 джобы — тесты `brain/`
  (unittest), сборка прошивки (`arduino-cli`, FQBN
  `esp32:esp32:XIAO_ESP32S3:PSRAM=opi`, secrets.h из example),
  сборка APK (`gradlew assembleDebug`, артефакт).
- `.editorconfig` — utf-8, отступы по типам, LF для `.githooks/*` и `*.sh`.
- `docs/onboarding.md` — инструкция новичку «с нуля» (Git/Python/Cursor,
  безопасность, прошивка, APK, мозг, git-workflow). Закрывает запрос из
  ранней Cursor-сессии.
- README: добавлены `brain/`, `onboarding.md`, `dev-log.md`, CI,
  security-runbook в «Состав».

### Статус верификации
`brain/`-тесты зелёные локально (9/9). Джобы прошивки/APK — стандартные
рецепты, реальный прогон — на CI/железе (в этой среде нет ESP32-core/SDK).

### Итог роадмапа
Фазы 1–5 выполнены автономно. Открыто (требует пользователя):
ротация Wi-Fi пароля + рерайт git-истории (`docs/security-history-cleanup.md`),
on-hardware верификация прошивки/APK, разводка УЗ/бамперов.

---

## 2026-05-17 — Фаза 4 (ИИ-«мозг»)

Новый компонент `brain/` (Python, stdlib; Pillow опц.). **Прошивка не
менялась** — переиспользован существующий HTTP-контракт + токен Фазы 1.
Транспортно-нейтрален (ПК-сервер или порт на телефон).

- `robot.py` — клиент платы (`/telemetry`, `/capture`, `/drive`, `?t=`).
- `perception.py` — `Recognizer` + `Dummy` (без зависимостей) + `Brightness`
  (Pillow опц., graceful fallback). Модель добавляется новой реализацией —
  архитектура от модели не зависит.
- `safety.py` — `SafetyGovernor` (чистая функция `decide`): рефлекс прошивки
  приоритетен, УЗ-стоп (`us_cm==0`≠препятствие), таймаут перцепции,
  неуверенность → стоп; темп команд `min(0.25, watchdog/2)` < 450 мс.
- `brain.py` — CLI + цикл perceive→decide→govern→act; аварийный `stop()`
  при потере связи и в `finally`; `--dry-run/--once` для офлайн-проверки.
- `test_safety.py` — 9 юнит-тестов governor.
- `docs/brain-api.md` — контракт и модель безопасности; `brain/README.md`;
  обновлён корневой README.

### Статус верификации
**Проверено здесь:** `python -m unittest discover -s brain` → 9/9 OK;
`brain.py --dry-run --once` проходит весь контур офлайн (корректный tank-mix).
На железе с платой — шаг пользователя.

### Дальше
Фаза 5 — гигиена/релиз: LICENSE, CI (arduino-cli + gradle + brain-тесты),
инструкция для новичка (Cursor/Python/Git), `.editorconfig`, релиз APK.

---

## 2026-05-17 — Фаза 3 (сенсоры и базовая автономность)

Код сделан корректным и устойчивым, **по умолчанию инертен** (УЗ/бамперы
выключены пинами `0`). Физическая разводка, выбор GPIO и проверка на плате —
шаг пользователя (см. `drive_config.h.example`); собрать/протестировать в этой
среде нельзя.

- `drive_config.h(.example)`: добавлены `DRIVE_ENC_DEBOUNCE_US` (антидребезг
  энкодера, по умолч. 150 мкс) и `DRIVE_US_STOP_CM` (порог рефлекса по УЗ);
  в `xiao_drive.h` — защитные `#ifndef` дефолты.
- Энкодеры: антидребезг в ISR по `micros()` (отбрасывает слишком близкие
  фронты); добавлен расчёт скорости `spd_l/spd_r` (тики/с).
- Рефлекс безопасности: единый латч (бампер | близкое УЗ), перекрывает
  команды клиента через гейт в `driveApplyMotors` (рефлекс важнее клиента),
  авто-сброс при пропадании препятствия. `us_cm==0` (нет эха/вне диапазона)
  НЕ считается препятствием — нет ложного стопа.
- УЗ читается до оценки безопасности; `pulseIn` троттлится ~120 мс.
- `/telemetry`: добавлены `spd_l`, `spd_r`, `drive_safety`.
- Android `TelemetryPoller`+`MainActivity`: компактная сводка сенсоров
  (СТОП-рефлекс/бампер/УЗ см/enc/v) в строке статуса.

### Дальше
Фаза 4 — ИИ-«мозг»: компонент `brain/` (Python, транспортно-нейтральный —
сервер или телефон), контракт API (`docs/brain-api.md`), safety-governor
(темп команд ≤ watchdog, аварийный стоп при потере связи), заглушка-распознавание.

---

## 2026-05-17 — Фазы 1–2

### Фаза 1 — Безопасность (автономная часть готова)
- Утёкший Wi-Fi пароль вырезан из архива Cursor
  (`chat-67f50fc7…jsonl`, 8 вхождений → `***REDACTED***`), zip пересобран;
  рабочее дерево и zip чисты.
- `.githooks/pre-commit` — блокировка секретов; раздел «Безопасность» в README.
- Опциональный `XIAO_API_TOKEN`: `apiAuthOk()` на `/drive` и `/control`
  (пустой = выключено, обратная совместимость с прокси/tooling);
  проброшен в Android (поле «Токен», SharedPrefs, `?t=`).
- **User-gated, НЕ выполнено:** ротация пароля роутера + рерайт git-истории +
  force-push (`docs/security-history-cleanup.md`). Пароль остаётся в истории
  коммита `b283eab` до ручной чистки владельцем репозитория.

### Фаза 2 — Надёжность ядра (готово)
- Android `MicPlayer`: bounded connect, авто-reconnect с backoff 0.8→4 с,
  корректный `stop()`, статус отражает реальное состояние.
- Android `MjpegStream`: конечный `readTimeout` 6 с (не виснет, реконнект);
  при переполнении буфера сохраняем хвост от последнего SOI; убран дубль-импорт.
- Android `DriveClient`: ошибки сети/HTTP-код выводятся в статус по смене
  состояния (видно «нет связи» / 403 неверный токен), без спама на 120 мс.
  `TelemetryPoller` намеренно тихий (его сбой виден как устаревший Wi-Fi).
- Поворот экрана: уже покрыт манифестом (`portrait` + `configChanges`) —
  кода не требуется.
- Прошивка: `initCamera()` — 3 попытки + `esp_camera_deinit` + восстановление
  статус-LED на провале; счётчик `cam_fail` в `/telemetry`.
- Прошивка: освобождение I2S при `/control?mic=0` (`telemetryMicApplyState`,
  безопасно к Core 1 `micTcpTask` через гейты `gCtrlMicEnabled`/`gMicOk`).

### Статус верификации
Прошивку (`arduino-cli`) и APK (Android SDK/JDK 17) в текущей среде собрать
нельзя и без платы не протестировать — компиляция/прогон вынесены в шаг
верификации на железе (`tools/xiao_flash_and_telemetry.ps1`;
`gradlew assembleDebug`). Код выверен по месту, по существующим паттернам.

### Дальше
Фаза 3 — сенсоры и базовая автономность: УЗ HC-SR04 + бамперы, debounce
энкодеров + одометрия в `/telemetry`, аппаратный рефлекс «стоп при
препятствии», индикаторы сенсоров в Android.
